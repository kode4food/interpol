let a = "hello"
let b = (
  name = 'Thom',
  age = 42,
  occupation = 'coder'
)

for item in b
  <b>"-- %name -- = -- %value --" % item</b>
end

let c = ('red', 'green', 'blue')
for color in c when color != 'green'
  <b>color</b>
end

for color in c when color == 'orange'
  <b>color</b>
else
  <b>"no color found"</b>
end

patternTest(true, "hello", "there")
patternTest(false, "goodbye", "now")
patternTest((name='bill', type='happy'), "so", "buttons")
patternTest(('red', 'green', (name='yellow', type='fred')), "yello", "doo")
patternTest("hello", "really", "i mean it")

b like (occupation='coder')


def patternTest((a), arg1, arg2)
  "Came in with 'hello' and %arg1 - %arg2"
end

def patternTest(true, arg1, arg2)
  "Came in with true and %arg1 - %arg2"
end

def patternTest(false, arg1, arg2)
  "Came in with false and %arg1 - %arg2"
end

def patternTest((type='happy'), arg1, arg2)
  "Came in with object and %arg1 - %arg2"
end

def patternTest(('red', 'green', (name='yellow')), arg1, arg2)
  "Came in with mixed and %arg1 - %arg2"
end

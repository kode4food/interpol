from layout.main import mainLayout
from partials.stooges import renderList

# partials are hoisted, so this can be here
mainLayout(title, content)

def content
  if people.length
    "There are %length stooges" % people

    # could have also been written:
    #   "There are" people.length "stooges"

    renderList(people)
  else
    "Um, no people?"
  end
end
